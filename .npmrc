# npm 12 refuses git dependencies by default (EALLOWGIT). @frc2064/ui is
# installed straight from GitHub rather than a registry, so that has to be
# turned back on -- scoped to `root`, which permits only the git dependencies
# this project declares itself, not any a transitive dependency might pull in.
allow-git=root
