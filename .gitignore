node_modules/
.svelte-kit/
build/
.vercel/
.env
.env.*
!.env.example
.DS_Store

# Agent tooling
.claude/
.agents/
.superpowers/
skills-lock.json

# This repository is public, and everything below carries real names, emails
# and phone numbers. Production snapshots, the team's raw sheet exports, and
# the seed payload generated from them: all cutover scaffolding whose
# generator and import panel are gone, kept only as local rules so a stray
# copy cannot be committed.
snapshots/
scripts/import/
src/lib/data/
